import { SetStateAction } from "react";
export type Setter = {
    (action: SetStateAction<number>, ignoreClamp?: boolean): void;
};
/** Like use state, but the number is bounded to `[min, max]` (closed interval) */
export declare function useClamped(start: number, min: number, max: number): [number, Setter];
