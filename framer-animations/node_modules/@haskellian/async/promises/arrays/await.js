export function await2d(xxs) {
    return Promise.all(xxs.map(xs => Promise.all(xs)));
}
export function await3d(xxxs) {
    return Promise.all(xxxs.map(xxs => Promise.all(xxs.map(xs => Promise.all(xs)))));
}
