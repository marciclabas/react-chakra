export async function* map(xs, f) {
    let i = 0;
    for await (const x of xs)
        yield f(x, i++);
}
export async function* filter(xs, p) {
    let i = 0;
    for await (const x of xs)
        if (p(x, i++))
            yield x;
}
